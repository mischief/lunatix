# SPDX-License-Identifier: ISC
# less is the name people type; the pager itself is luaposixcli's more,
# which POSIX names.
install_symlink('less',
  install_dir: get_option('bindir'),
  pointing_to: 'more',
)
